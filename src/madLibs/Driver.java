package madLibs;

public class Driver {
	/* To start we need to first set up a MadLib which will be performed in the MadLib class
	 * Then the object will be passed through to the interpreter which will find all the blanks and pass instructions to the user
	 * Instructions from the user will be sent through the scanner class and added into the array
	 * At the end, the driver will display the array
	 * 
	 */
}
